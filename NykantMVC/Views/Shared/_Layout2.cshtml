<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
</head>
<body>
    @foreach (var bagItem in Model)
    {
        <img src="@bagItem.Product.ImageSource" editable="true" />
    }
    <div>
        @RenderBody()
    </div>
</body>
</html>
